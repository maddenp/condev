{% set info = load_file_data("hello/resources/meta.json") %}

package:
  name: {{ info["name"] }}
  version: {{ info["version"] }}
source:
  path: ../src
build:
  include_recipe: false
  number: {{ info["buildnum"] }}
requirements: {{ info["requirements"] }}
test: {{ info["test"] }}
about:
  license: OTHER
